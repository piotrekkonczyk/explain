You will be given either a single Python function or an entire Python file containing multiple functions and classes.

Your task is to add informative and well-formatted docstrings to all top-level functions and classes provided in the input.

Each docstring should include:
- A brief summary of what the function or class does
- Descriptions of parameters
- A description of the return value (respect type hints if available)

Assume that all used APIs or libraries are valid and do not need to be explained. Focus only on describing the behavior and purpose of the given functions or classes, not their internal dependencies.

Return the complete modified input in a ```python markdown block. Preserve all original code and formatting — only insert docstrings where needed.
